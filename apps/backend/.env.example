# Backend Environment Variables
# Copy this file to .env and fill in the actual values

# Server Configuration
NODE_ENV=development
PORT=3000

# Database
DATABASE_URL=postgresql://user:password@localhost:5432/asyncstand

# Authentication
JWT_SECRET=your-super-secret-jwt-key-here

# CORS Configuration (optional)
# Frontend URL for production/staging
# FRONTEND_URL=https://asyncstand-frontend-prod.onrender.com

# Allowed origins (comma-separated) - Add ALL your domains here
# CORS_ALLOWED_ORIGINS=https://asyncstand.com,https://staging.asyncstand.com,https://api.asyncstand.com,https://staging.api.asyncstand.com,https://app.asyncstand.com,https://staging.app.asyncstand.com,https://asyncstand-frontend-prod.onrender.com,https://asyncstand-frontend-staging.onrender.com,https://asyncstand-backend-prod.onrender.com,https://asyncstand-backend-staging.onrender.com

# Origin patterns (comma-separated regex patterns) - For dynamic matching
# CORS_ALLOWED_PATTERNS=^https:\\/\\/[\\w-]+\\.asyncstand\\.com$,^https:\\/\\/asyncstand-frontend[\\w-]*\\.onrender\\.com$,^https:\\/\\/asyncstand-backend[\\w-]*\\.onrender\\.com$

# Ngrok URL for development tunneling (optional)
# NGROK_URL=https://your-subdomain.ngrok-free.app

# CSRF Configuration
# Public endpoints that don't require CSRF protection (comma-separated)
# CSRF_PUBLIC_ENDPOINTS=/auth/signup,/auth/forgot-password,/auth/reset-password,/webhooks/stripe